import 'package:test/test.dart';

import 'constraint_unit_tests.dart';

void main() {
  group('Unit Tests', () {
    group('// 1. Constraints //', () {
      executeConstraintUnitTests();
    });
  });
}

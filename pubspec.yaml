name: db_migrator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  

dev_dependencies:
  postgres: ^2.4.5
  lints: ^2.0.0
  test: ^1.16.0
